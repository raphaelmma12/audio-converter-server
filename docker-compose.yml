version: '3.8'

services:
  audio-converter:
    build: .
    container_name: audio-converter
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
      - ALLOWED_ORIGINS=http://localhost:8080
    restart: unless-stopped
